# Q1 Clustering Phase-1 交接

## 为什么研究聚类

当前 Q1 B1 从单设施 B0 出发，按设施最小距离为每条路线保留同机场、容量兼容的 Top-5 邻居，再用完整路线评价器联合枚举服务顺序、机型、技术经停与加油方案。聚类的假设是：全局设施结构可能比局部距离更早发现优质合并，在固定评价预算下改善解或减少精确评价次数。

聚类只用于候选排序，不参与容量、燃油、停靠或 Validator 判定；跨簇候选始终保留。

## 数据是否支持

- 52 个设施的两两距离中位数为 157 km，最近邻距离中位数为 18 km，存在明显局部结构。
- 25 km 阈值下有 18 个连通分量，50 km 时已连成一个整体，不支持稳定的密度聚类。
- 原始矩阵存在 147 个无序节点对、442 个三角不等式违例，因此直接使用题面不相似度，不作最短路闭包，也不把嵌入坐标解释为真实地图。
- 已接受 B1 合并的设施距离、机场轮廓差和燃油签名差均低于容量兼容但未接受对，但样本受原搜索过程选择，不能作为监督标签。

因此 Phase-1 聚类对象确定为 52 个设施；路线、OD 和机场不单独聚类。设施对 compatibility 仅作为未来日志分析对象。

## 已实现方法与接口

- 纯 Python、确定性 PAM K-Medoids；
- 基于预计算距离的 average-linkage 层次聚类；
- `K=2…10` 的 silhouette、簇内不相似度、簇规模及 50 次 ±5% 距离扰动 ARI；
- `ClusterResult`、`RelatednessModel` 和可插拔 `CandidateRanker`；
- raw-distance、PAM、average 三种 ranker；
- legacy Top-N、全局固定预算和全候选三种候选模式；
- 候选级日志：排名、是否入预算、距离/机场/燃油特征、路线评价、技术经停调用、缓存、节约值和最终接受状态；
- V1/V2 所需的经验百分位归一化与组合不相似度接口，但未在 V0 失败后继续调权。

默认 legacy 路径精确复现原 B1：15,743 飞机分钟、738 个路线对、2,112 次路线评价、10 次合并，1600/1600、Validator PASS。

## A/B 设计与结果

所有方案从同一 B0 出发，使用相同评价器、目标、缓存策略、停止规则和 Validator；仅改变候选排序。固定每轮精确评价预算为 25/50/75，并执行全候选负控制。

| 方案 | 预算 | 飞机时间/min | 人员时间/min | 架次 | 路线评价 |
|---|---:|---:|---:|---:|---:|
| raw distance | 25 | **15,683** | 120,949 | **99** | **636** |
| raw distance | 50 | 15,743 | 120,396 | 100 | 1,192 |
| raw distance | 75 | 15,743 | 120,396 | 100 | 1,620 |
| PAM K3 | 25 | **15,683** | 120,949 | **99** | 612 |
| PAM K3 | 50 | 15,743 | 120,396 | 100 | 1,138 |
| PAM K3 | 75 | 15,743 | 120,396 | 100 | 1,564 |
| average K3 | 25 | **15,683** | 120,949 | **99** | 642 |
| average K3 | 50 | **15,683** | 120,949 | **99** | 1,196 |
| average K3 | 75 | 15,775 | 120,692 | 100 | 1,642 |

PAM K3 与 raw 得到相同解，仅减少约 3.5%–4.5% 路线评价，低于 10% 辅助门槛。average K3 只在预算 50 改善，预算 75 反而恶化，不满足“至少两个预算改善且其余不变差”。

五次计时的端到端中位数为：raw K50 24.22 s、PAM K3 27.04 s、average K3 27.82 s。聚类分别慢 11.6% 和 14.9%。三种全候选审计均得到 15,743 分钟、776 对、2,320 次路线评价，证明排序没有改变候选合法性或目标函数。

全部实验均覆盖 1600 人、内部指标与 Validator 一致且 Validator PASS。

## 决策与下一步

**决策：V0 聚类不进入当前 Generalized Savings 主线，也不继续 V1/V2 权重搜索。** 内部 silhouette 良好不等于下游有收益；现有 raw distance 已捕获绝大多数相关结构。

本阶段最有价值的新结果来自非聚类对照：全局 raw-distance 预算 25 将飞机时间从 15,743 降到 15,683，架次从 100 降到 99，同时路线评价降至 636。该结果应作为 classical solver 的独立候选改进，由主线复核并与正在开发的 batch relocation/ejection 合并测试。

聚类模块暂保留在独立分支作为研究工具和未来 ALNS related-removal 接口，不合入当前核心求解器。只有后续 Local Search/ALNS 产生新的动态邻域、并能用独立 A/B 证明收益时，才重新评估设施 relatedness；监督学习和 Bandit 继续等待足够的候选/算子日志。

完整实验产物位于 `outputs/q1/clustering/20260814-phase1-screen/`，包括 cluster membership、内部指标、每个配置的官方 CSV、Validator、候选事件、A/B 汇总和决策表。
