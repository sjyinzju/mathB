# 新版 `main.tex` 修改说明

## 1. 图表中文化

- 从 `code/outputs/q1/alns_best/q1-convergence.csv` 的 120 轮原始日志重新绘图，没有修改曲线数据。
- 将横纵轴、图例、阶段名和终值标注全部改为中文。
- 图中保留总飞机使用时间理论下界 13337 min，并用竖直点线标出两个搜索阶段的分界。
- 新图位于 `figures/q1_alns_convergence.pdf`，同时提供 PNG 预览。

## 2. 问题一模型与程序对齐

- 将原来的笼统“节约算法 + ALNS”改成仓库实际执行的 B0--B1--B2 三级流程：
  1. B0：单设施容量动态规划与 LAND 分流；
  2. B1：广义节约合并；
  3. B2：带 SciPy MILP 精确修复的两阶段 ALNS。
- 更正 LAND 分配描述：同一设施的 LAND 人员可以拆分至多个机场，不再写成全部分给单一最近机场。
- 更正容量动态规划递推：新增架次的载客人数 `\lambda` 在可行范围内枚举，不固定为 `min(n,s_k)`。
- 更正燃油标签状态：标签包含位置、停靠次数、累计时间、剩余燃油和累计耗油，支配规则同步考虑这些量。
- 删除程序中并不存在的 Regret 插入、2-opt、swap 等问题一修复描述，改为五类整路线破坏算子、候选路线生成和整数规划修复。
- 加入两阶段真实参数：两阶段各 60 轮，移除 2--3/3--4 条路线，单次修复时限 3/5 s；总实测时间 985.895 s。

## 3. 符号统一

- 将 `\lambda_k(n)` 改为枚举装载量 `\lambda`。
- 新增 LAND 分流变量 `\omega_{af}`。
- 新增 ALNS 修复符号 `\mathcal G_D`、`\mathcal R_D^{\mathrm{cand}}`、`Q_g^D`、`\zeta_c`、`\chi_{gc}`、`\vartheta_{cg}`。
- 新增符号只用于问题一整数修复，不改变问题二、三原有符号含义。

## 4. 真实结果替换占位符

| 阶段 | 飞机时间/min | 人员在途/min | 架次数 | 燃油/kg | 利用率 |
|---|---:|---:|---:|---:|---:|
| B0 | 17222 | 120609 | 110 | 142601.0 | 43.047% |
| B1 | 15743 | 120396 | 100 | 128562.3 | 46.757% |
| B2 | 15418 | 121138 | 95 | 124811.4 | 48.696% |

- B2 相对 B1 的总飞机使用时间下降 325 min，即 2.064%；架次数减少 5，燃油减少 3750.9 kg，利用率提高 1.939 个百分点。
- 人员在途时间由 120396 增至 121138 min 并非计算错误：论文采用严格词典序目标，只要一级目标总飞机时间下降，就允许二级目标暂时变差。
- 最终方案覆盖 1600 人，独立 Validator 结果为 `valid=true`、`issue_count=0`。

## 5. 理论界与算法证据

- 加入飞机时间、人员时间、架次数、燃油和利用率的理论界对照表。
- 最终总飞机时间相对下界 13337 min 的保守间隙为 15.603%。这些分量界在不同放松条件下取得，不能理解为同一个可行方案能够同时达到。
- 删除没有实际实验数据的“消融表”和“至少 10 个随机种子统计表”。
- 改用真实 `operator_stats.csv`：低利用率路线移除产生 9 次历史最优，是本次运行中贡献最大的算子。

## 6. 摘要、结论和附录

- 摘要和结论已写入问题一真实指标与改进率。
- 问题二、三现均已有实算结果、图表、界限和独立校验，摘要与结论已同步最终数字。
- 附录代码路径已改成仓库真实文件名，并在本包 `code/` 中附上三问运行程序、数据、结果和测试。
- AI 工具使用声明改成当前工作中实际发生的用途；提交前仍应由参赛队核对并按比赛要求如实确认。

## 7. 编译

在本目录执行：

```bash
latexmk -xelatex -bibtex main.tex
```

必须使用 XeLaTeX；`ctexart` 的 `zihao=-4` 是小四号正文。如果希望正文整体增大一号，可改成 `zihao=4`，不要写 `zihao=小四` 或在 `ctex` 选项中换行拆开 `zihao`。

## 8. 尚需小组完成的内容

- 在安装完整CTeX宏包的本地环境编译并核对30页限制；
- 参赛队号、支撑材料命名和AI使用详情的最终人工核对；
- 若时间允许，补做Q3完整深度算法的多次重复运行统计。

## 9. Q3增强保守界限（v7）

- 新增 `code/src/solver/q3_bounds.py`，实现机场—机型分层连续多商品流全局松弛；
- 新增 `code/scripts/08_compute_q3_enhanced_bounds.py`，重算界限并同步JSON摘要；
- 原问题有效下界由12389 min提高到14125 min，认证区间由59.441%缩小到53.758%；
- 另求得有限候选路线池参考界15198 min，并在正文中明确其不具备全局证明效力；
- 新增回归测试与 `q3-enhanced-bounds.json`，附录代码路径已同步。

## 10. Q3 P0--P1结构优化（v8）

- 修正两阶段反馈逻辑：旧Stage 2删除临时乘客后正式成为Stage 1候选；
- 新增11种构造模式、Top-K接口和Hard-Skeleton/Flexible-Regret模式；
- 新增海上等待时间感知排班器及实际时刻导出；
- 新增同日2→1多航班Ruin-and-Recreate，真实接受两次合并；
- Stage 1由30546降至30180 min，架次173降至171，燃油降至239702.3 kg；
- 新上界下Stage 2服务158/160名临时人员，固定171架次分配模型内达到最优；
- 全局界14125 min保持不变，认证区间更新为53.197%；
- `main.tex`、中文图、附录路径、结果报告与评价报告均已同步。

## 11. Q3 Closure--P2闭环优化（v9）

- 不再依赖外部仓库，以v8随包数据、路线缓存和结果为唯一基线；
- 具体飞机排班改用实际到离站时刻、允许海上等待并严格检查30 min周转；
- 采用11种确定性构造模式加1个固定种子随机启动，对Top-3多样候选深度搜索；
- 实现真实硬任务骨架--柔性Regret构造，失败启动保留为诊断证据；
- 将同日2→1扩展为2--4条旧航班到可变替代数的广义$k\to m$重构；
- Closure接受一次真实3→2重构，节约211 min，将171架次降至170；
- 增加P2A定向可行性档案、P2B五算子ALNS、P2C动态路线增广和P2D跨日搜索；
- 执行必需的Stage 2删除临时人员后投影反馈，最终两阶段共用29659 min、168架次结构；
- Stage 1相对旧30546 min基准减少887 min、5架次和6278.3 kg燃油；
- Stage 2在同一上界下服务158/160名临时人员，固定168架次分配MILP内达到最优；
- 全局有效下界保持14125 min，认证区间更新为52.375%；
- 12项回归测试全部通过，两阶段最终CSV均由独立Validator复算为0违规；
- `main.tex`、中文图、附录路径、复现说明与获奖潜力报告已统一为v9数字。

## 12. Q1/Q2队友优化集成（v10）

- 以v9为底稿，保留Q3 Closure--P2代码、结果和论文内容；
- Q1正式结果改用`outputs/q1/final/`，而不是压缩包内旧`best/`；
- Q1加入原始设施距离、Context V2候选排序、公平五种子和标准ALNS反馈精修；
- Q1由15418降至14770 min，架次95降至89，理论保守区间缩至10.745%；
- Q2加入4路线精确局部MILP、1--5设施长列、精英重组、全局重启和交叉交换；
- Q2由19736降至17595 min，架次107降至96，利用率升至91.021%；
- 明确16296只属于旧有限候选RMP，5.8651%只属于最终局部邻域；
- 从Q2的1.76 GB解压内容中排除巨型候选日志，只保留源代码、测试、正式结果和必要复现文件；
- 新增共享`cache.py`与`physics.py`，并手工合并求解器导出接口，未覆盖Q3模块；
- Q1、Q2、Q3两阶段四份正式CSV均在集成代码下重新验证为0违规；
- 新增Q1/Q2中文阶段图、压缩包分析报告、v10复现说明和新版获奖潜力评价。
# v11：Q1 Final-OR 与 Q2 Round-3 集成

- Q1 正式解由 14770 更新为 14730 min；新增精英路线池、allocated-pattern 精确主问题、有限路线池 LP 和标准 ALNS education 的论文与代码说明。
- Q2 正式解由 17595 更新为 17218 min，架次数由 96 更新为 95；新增停滞检查点、4→3 吸收、低频 5 路线和 Round-3 审计说明。
- 更新摘要、符号表、Q1/Q2 结果表、理论界限、消融、代表性路线、模型局限、结论和附录代码目录。
- 保留 v10 正式 Q1/Q2 结果为 `code/outputs/q1/v10-final-14770/` 和 `code/outputs/q2/v10-best-17595/`，便于回滚与对照。
- 保留 v9/v10 的 Q3 Closure--P2 代码与 29659 min 正式结果不变。
- 重新执行 Q1、Q2、Q3 正式 CSV 独立校验，均为 0 issues。
